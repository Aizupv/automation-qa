//Task 4.1
let radius = 20;
let squere = Math.pow(radius , 2) * Math.PI; // Перший варіант
let squere1=Math.PI * (radius ** 2); //Другий варіант
console.log(squere.toFixed(2) , squere1.toFixed(2));

//Task 4.2
let lenght = 10;
let widht = 15;
let rectangleSquere = lenght * widht;
console.log(rectangleSquere.toFixed(2));

//Task 4.3
let cilindreRadius = 15;
let cilindreHeight = 12;
let cilindreVolume = Math.PI * Math.pow(cilindreRadius , 2) * cilindreHeight;
let cilindreVolume1 = Math.PI * (cilindreRadius ** 2) * cilindreHeight //Другий варіант
console.log(cilindreVolume.toFixed(2))
